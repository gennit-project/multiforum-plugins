{
  "name": "multiforum-plugins",
  "version": "2.0.0",
  "description": "Registry source list for Multiforum standalone plugin releases",
  "private": true,
  "type": "module",
  "scripts": {
    "registry:generate": "node scripts/generate-registry.mjs",
    "test:registry": "node --test scripts/generate-registry.test.mjs",
    "test": "npm run test:registry"
  },
  "engines": {
    "node": ">=18"
  }
}
